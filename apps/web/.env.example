# mock: start the in-browser MSW API; real: call the FastAPI service.
VITE_API_MODE=mock

# Optional for a separately hosted API. Leave empty for same-origin /api requests.
# VITE_API_URL=http://localhost:8000
