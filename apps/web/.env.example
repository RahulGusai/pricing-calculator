# `real` is the default application path. `mock` is limited to explicit visual
# exploration; production never starts the browser mock worker.
VITE_API_MODE=real

# Railway uses the Caddy same-origin /api proxy. Vite development calls FastAPI
# directly and therefore needs the local API origin.
VITE_API_URL=http://localhost:8000
